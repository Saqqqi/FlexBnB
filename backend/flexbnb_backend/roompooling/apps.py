from django.apps import AppConfig


class RoompoolingConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'roompooling'
    verbose_name = 'Room Pooling & Cost Sharing'

